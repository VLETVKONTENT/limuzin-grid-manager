# v0.9.0 — Release Candidate

Дата: 2026-04-07

Статус: рабочая версия.

## Что изменено

- Обновлена версия приложения до `0.9.0` в `pyproject.toml`, `src/limuzin_grid_manager/__init__.py` и `version_info.txt`.
- Добавлена пользовательская инструкция `USER_GUIDE.md` с обычным рабочим сценарием и быстрым чеклистом проверки.
- Обновлены `README.md`, `GRIDBASE.md`, `roadmap.md` и `versions/GRIDVERSIONS.md` под `v0.9.0`.
- Уточнена подсказка кнопки `Настроить стиль KML`: диалог управляет линиями и заливкой как `1000x1000`, так и `100x100`.

## Что проверено

- Полный набор автотестов ядра, экспорта, форматов экспорта, проектов и UI.
- Компиляция `src` и `tests`.
- Release-candidate smoke-проход: общий KML, ZIP по `1000x1000`, экспорт только `100x100`, пользовательский KML-стиль, все схемы нумерации `100x100` на матрице `10x10`, сохранение и открытие `.lgm.json`.
- Сборка `dist/LIMUZIN_GRID_MANAGER.exe` с версией `0.9.0.0`.
- Smoke-запуск EXE: процесс стартует и не завершается сразу.
- Ручной тест версии пользователем пройден.

Проверки:

```powershell
uv lock --offline
uv run --offline --extra dev pytest
uv run --offline --extra dev python -m compileall src tests
$env:UV_OFFLINE='1'; .\build_exe_windows.bat
```

Результат автопроверок: `42 passed`.
